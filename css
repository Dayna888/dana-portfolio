.sparkle {
  position: absolute;
  width: 10px;
  height: 10px;
  background-color: pink;
  border-radius: 50%;
  opacity: 0.7;
  animation: sparkle-animation 1s ease-out;
}

@keyframes sparkle-animation {
  0% {
    transform: scale(1);
  }
  100% {
    transform: scale(3);
    opacity: 0;
  }
}
